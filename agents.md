# Agent Instructions

- Treat `spec.md` as the source of truth for this project.
- Whenever any work is performed, review `spec.md` and update it in the same change so that it remains consistent with the current requirements, behavior, architecture, interfaces, and implementation status.
- Do not consider work complete while `spec.md` is stale. If a change does not alter the specification, record any relevant implementation-status update; otherwise explicitly verify that no documentation change is needed.
- Keep documentation concise, accurate, and free of speculative claims.

